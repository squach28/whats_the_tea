// model for a user when viewed on the chats screen

class ChatUser {
  String name;
  String messageText;
  String imageURL;
  String time;

  // ChatUsers({@required this.name, @required this.messageText, @required this.imageURL, @required this.time});
}
